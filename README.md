# CloudPlare

CloudPlare is a Korean cloud operations notes site for small website operators.

It focuses on DNS, static deployments, HTTP caching, CDN behavior, Core Web Vitals, HTTPS redirects, Next.js deployment notes, and incident logging.

## Development

```bash
npm run dev
npm run build
npm run lint
```

The production domain is `https://cloudplare.com`.
